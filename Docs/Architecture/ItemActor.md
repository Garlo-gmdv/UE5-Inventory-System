\# Initialization

* The item actor requires an additional Initialize function to initialize the values after a SpawnActor in C++.
* This does not concern the blueprint as it uses the "ExposeOnSpawn" metadata specifier.

